apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configMap.name }}
  namespace: {{ .Values.appNamespace }}
data:
  index.html: |
    <h1> Hello Backend User. Request Receives within <strong> Backend </strong> </h1>
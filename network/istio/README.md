# FOR YOUR INFORMATION:
### Istio APPs are actually copied from ISTIO example's APPs but some modifications were done for learning purpose nothings else
# Istio APP deployed 

## Detail APP
Helm installation cmd

```shell
helm install istio-stg -f values-stg.yaml .
```
Helm uninstallation cm

```shell
 helm uninstall istio-stg
```
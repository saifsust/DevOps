plugins{
    id 'base'
}

tasks.register("CopyDockerfile", Copy as Class<Task>){
    Copy it ->{
        it.from("source/Dockerfile")
        it.into("../docker")
        println 'Successfully copied Dockerfile'
    }
}


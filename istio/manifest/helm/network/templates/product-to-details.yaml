apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: product-to-detail
  namespace: {{ .Values.appNamespace }}
spec:
  endpointSelector:
    matchLabels:
      app: {{ .Values.network.product }}
  egress:
    - toEndpoints:
        - matchLabels:
            app: {{ .Values.network.detail }}
      toPorts:
        - ports:
            - port: "9080"
              protocol: TCP
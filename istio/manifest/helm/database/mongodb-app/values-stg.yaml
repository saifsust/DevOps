appName: book-mongodb-app
appNamespace: graphql-system

service:
  name: mongodb-app-svc
  type: ClusterIP
  # nodePort: 31108

deployment:
  replicaCount: 1
  maxSurge: 40%
  maxUnavailable: 10%
  pod:
    label: mongodb-app
    version: "1.0.98"
    port: 27017


apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.application.name }}
  namespace: {{ .Values.targetNamespace }}
spec:
  project: {{ .Values.project.name }}
  source:
    repoURL: {{ .Values.application.source.repoURL }}
    targetRevision: {{ .Values.application.source.targetRevision }}
    path: {{ .Values.application.source.path }}
    helm:
      valueFiles:
        - {{ .Values.application.source.helm }}
  destination:
    server: {{ .Values.application.server }}
    namespace: {{ .Values.application.namespace }}
  syncPolicy:
    automated:
      prune: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=tue
      - Replace=true

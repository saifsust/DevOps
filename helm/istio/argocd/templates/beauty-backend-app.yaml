apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.backend.name }}
  namespace: {{ .Values.targetNamespace }}
spec:
  project: {{ .Values.project.name }}
  source:
    repoURL: {{ .Values.backend.source.repoURL }}
    targetRevision: {{ .Values.backend.source.targetRevision }}
    path: {{ .Values.backend.source.path }}
    helm:
      valueFiles:
        - {{ .Values.backend.source.helm }}
  destination:
    server: {{ .Values.backend.destination.server }}
    namespace: {{ .Values.backend.destination.namespace }}
  syncPolicy:
    automated:
      prune: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=tue

# Argo Workflow reverse proxy
server{
    listen 80;
    server_name argoworkflow.uddom.com;

    location / {
       proxy_pass https://192.168.0.140:32746;

       proxy_http_version 1.1;

       proxy_ssl_verify off;

       proxy_set_header Host $host;
       proxy_set_header X-Forwarded-Proto $scheme;
       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
# Argo CD reverse proxy
server{
    listen 80;
    server_name argocd.uddom.com;

    location / {
       proxy_pass http://192.168.0.140:30080;

       proxy_http_version 1.1;

       proxy_ssl_verify off;

       proxy_set_header Host $host;
       proxy_set_header X-Forwarded-Proto $scheme;
       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
# Elastic search proxy route
server{
    listen 80;
    server_name elasticsearch.uddom.com;

    location / {
       proxy_pass http://192.168.0.140:30200;

       proxy_http_version 1.1;

       proxy_ssl_verify off;

       proxy_set_header Host $host;
       proxy_set_header X-Forwarded-Proto $scheme;
       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
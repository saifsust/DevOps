# Elastic search proxy route
server{
    listen 80;
    server_name prometheus.uddom.com;

    location / {
       proxy_pass http://192.168.0.145:9090;

       proxy_http_version 1.1;

       proxy_ssl_verify off;

       proxy_set_header Host $host;
       proxy_set_header X-Forwarded-Proto $scheme;
       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

server{
    listen 80;
    server_name grafana.uddom.com;

    location / {
       proxy_pass http://192.168.0.145:3000;

       proxy_http_version 1.1;

       proxy_ssl_verify off;

       proxy_set_header Host $host;
       proxy_set_header X-Forwarded-Proto $scheme;
       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

server{
    listen 80;
    server_name vault.uddom.com;

    location / {
       proxy_pass http://192.168.0.145:8200;

       proxy_http_version 1.1;

       proxy_ssl_verify off;

       proxy_set_header Host $host;
       proxy_set_header X-Forwarded-Proto $scheme;
       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}